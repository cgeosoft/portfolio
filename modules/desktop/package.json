{
  "name": "portfolio-desktop",
  "version": "0.5.1",
  "private": true,
  "type": "module",
  "scripts": {
    "stage": "bun run scripts/stage.ts",
    "dev": "bun run stage && electrobun dev",
    "build": "bun run stage && electrobun build --env=stable",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "portfolio-shared": "workspace:*"
  },
  "devDependencies": {
    "bun-types": "^1.3.14",
    "electrobun": "^2.0.1",
    "typescript": "^5.7.3"
  }
}
