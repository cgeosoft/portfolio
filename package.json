{
  "name": "portfolio",
  "version": "0.4.0",
  "author": "Christos Georgiou <christos@cgeosoft.com>",
  "private": true,
  "type": "module",
  "workspaces": [
    "modules/shared",
    "modules/service",
    "modules/gui",
    "modules/desktop"
  ],
  "scripts": {
    "dev": "bun --filter portfolio-service --filter portfolio-gui dev",
    "build": "bun --filter portfolio-service --filter portfolio-gui build",
    "start": "bun --filter portfolio-service start",
    "desktop": "bun run --cwd modules/desktop dev",
    "desktop:build": "bun run --cwd modules/desktop build",
    "build:metrics": "bun run scripts/build-metrics.ts",
    "check:metrics": "bun run scripts/build-metrics.ts --check",
    "build:metrics-site": "bun run scripts/build-metrics-site.ts",
    "test": "bun --filter portfolio-service --filter portfolio-gui --filter portfolio-desktop test",
    "typecheck": "bun --filter '*' typecheck",
    "release": "bash scripts/release.sh release",
    "release:build": "bash scripts/release.sh build",
    "release:publish": "bash scripts/release.sh publish-website",
    "website:publish": "bash scripts/release.sh publish-website"
  },
  "devDependencies": {
    "assemblyscript": "0.28.20",
    "bun-types": "^1.3.14",
    "typescript": "^5.7.3"
  }
}
