name: Release Packages

on:
  push:
    tags:
      - 'v*'
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      release_tag:
        description: 'Release tag to attach packages to (leave empty for build-only test run)'
        required: false
        default: ''

jobs:
  build-debian:
    name: Build Debian Package
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y dpkg zstd libwebkit2gtk-4.1-dev libgtk-3-dev

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Build and package Debian
        env:
          POSTHOG_API_KEY: ${{ secrets.POSTHOG_API_KEY || '' }}
          WEBPAGE_URL: ${{ secrets.WEBPAGE_URL || '' }}
        run: |
          VERSION="${{ github.event.release.tag_name || inputs.release_tag || github.ref_name || '' }}"
          VERSION="${VERSION#v}"
          if [ -z "$VERSION" ]; then
            VERSION="$(bun -e "console.log(require('./package.json').version)")"
          else
            bun pm version "$VERSION" --no-git-tag-version 2>/dev/null || npm version "$VERSION" --no-git-tag-version --allow-same-version 2>/dev/null || true
          fi
          echo "Building Debian package version $VERSION"
          bash scripts/release.sh debian --version="$VERSION"

      - name: Upload Debian artifacts
        uses: actions/upload-artifact@v4
        with:
          name: debian-package
          path: |
            dist/*.deb
            dist/*.tar.gz
            artifacts/*-Setup.tar.gz
          if-no-files-found: error

  build-windows:
    name: Build Windows Package
    runs-on: windows-latest
    defaults:
      run:
        shell: bash
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Build and package Windows
        env:
          POSTHOG_API_KEY: ${{ secrets.POSTHOG_API_KEY || '' }}
          WEBPAGE_URL: ${{ secrets.WEBPAGE_URL || '' }}
        run: |
          VERSION="${{ github.event.release.tag_name || inputs.release_tag || github.ref_name || '' }}"
          VERSION="${VERSION#v}"
          if [ -z "$VERSION" ]; then
            VERSION="$(bun -e "console.log(require('./package.json').version)")"
          else
            npm version "$VERSION" --no-git-tag-version --allow-same-version 2>/dev/null || true
          fi
          echo "Building Windows package version $VERSION"
          bash scripts/release.sh windows --version="$VERSION"

      - name: Upload Windows artifacts
        uses: actions/upload-artifact@v4
        with:
          name: windows-package
          path: |
            dist/*.exe
            dist/*.zip
            artifacts/*.exe
          if-no-files-found: warn

  build-macos:
    name: Build macOS Package
    runs-on: macos-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Build and package macOS
        env:
          POSTHOG_API_KEY: ${{ secrets.POSTHOG_API_KEY || '' }}
          WEBPAGE_URL: ${{ secrets.WEBPAGE_URL || '' }}
        run: |
          VERSION="${{ github.event.release.tag_name || inputs.release_tag || github.ref_name || '' }}"
          VERSION="${VERSION#v}"
          if [ -z "$VERSION" ]; then
            VERSION="$(bun -e "console.log(require('./package.json').version)")"
          else
            npm version "$VERSION" --no-git-tag-version --allow-same-version 2>/dev/null || true
          fi
          echo "Building macOS package version $VERSION"
          bash scripts/release.sh macos --version="$VERSION"

      - name: Upload macOS artifacts
        uses: actions/upload-artifact@v4
        with:
          name: macos-package
          path: |
            dist/*.dmg
            dist/*.zip
            artifacts/*.dmg
          if-no-files-found: warn

  publish-release:
    name: Publish Release Assets
    needs: [build-debian, build-windows, build-macos]
    runs-on: ubuntu-latest
    if: github.event_name == 'release' || github.event_name == 'push' || (github.event_name == 'workflow_dispatch' && inputs.release_tag != '')
    permissions:
      contents: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download all packages
        uses: actions/download-artifact@v4
        with:
          path: release-artifacts
          merge-multiple: true

      - name: Attach artifacts to GitHub Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG_NAME: ${{ github.event.release.tag_name || inputs.release_tag || github.ref_name }}
        run: |
          echo "Publishing artifacts for release tag: $TAG_NAME"
          mkdir -p release-files
          find release-artifacts -type f -exec cp -v {} release-files/ \;
          ls -lh release-files/

          # Use the changelog entry for this version as the release description.
          VERSION="${TAG_NAME#v}"
          NOTES_FILE="release-files/release_notes.md"
          if [ -f CHANGELOG.md ]; then
            awk -v v="$VERSION" '
              $0 ~ "^## \\[" v "\\]" { found=1; next }
              found && /^## \[/ { exit }
              found { print }
            ' CHANGELOG.md > "$NOTES_FILE"
          fi
          if [ ! -s "$NOTES_FILE" ]; then
            echo "Release $TAG_NAME" > "$NOTES_FILE"
          fi

          if gh release view "$TAG_NAME" >/dev/null 2>&1; then
            echo "Release $TAG_NAME exists, uploading/updating assets..."
            gh release upload "$TAG_NAME" release-files/* --clobber
            gh release edit "$TAG_NAME" --notes-file "$NOTES_FILE"
          else
            echo "Release $TAG_NAME not found, creating release..."
            gh release create "$TAG_NAME" release-files/* --title "$TAG_NAME" --notes-file "$NOTES_FILE"
          fi
